jdk:
  - openjdk17
before_install:
  - sdk install java 17.0.1-open
  - sdk use java 17.0.1-open
  - cd common  # Navigate to the common module
install:
  - ../gradlew :common:publishToMavenLocal  # Build and publish only common
